# Telegram Channel Monitor MVP

Приложение для мониторинга Telegram-каналов и поиска сообщений по заданным правилам.

## Функциональность

- 📺 **Управление каналами** - добавление, удаление, включение/выключение каналов для мониторинга
- 🔍 **Правила поиска** - создание правил поиска (ключевые слова, regex, семантический поиск)
- ✅ **Просмотр совпадений** - фильтрация и просмотр найденных совпадений
- 📊 **Статистика** - общая статистика системы
- 🤖 **Интеграция с Telegram** - использование бота для уведомлений

## Установка

```bash
npm install
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## Деплой на Vercel

### 1. Подготовка

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login
```

### 2. Настройка переменных окружения

В настройках проекта Vercel добавьте переменную окружения:

- `TELEGRAM_BOT_SERBIA` - токен Telegram бота (уже есть в Render)

### 3. Деплой

```bash
# Из корневой папки проекта
vercel

# Или для production
vercel --prod
```

### 4. Настройка Cron Jobs

Cron job для автоматического сканирования каналов настроен в `vercel.json`:
- Запускается каждый час (`0 * * * *`)
- Endpoint: `/api/scan`

## API Endpoints

### Каналы
- `GET /api/channels` - список всех каналов
- `POST /api/channels` - добавить новый канал
- `PATCH /api/channels/:id` - обновить канал
- `DELETE /api/channels/:id` - удалить канал

### Правила поиска
- `GET /api/rules` - список всех правил
- `POST /api/rules` - создать новое правило
- `PATCH /api/rules/:id` - обновить правило
- `DELETE /api/rules/:id` - удалить правило

### Совпадения
- `GET /api/matches` - список совпадений
  - Query параметры:
    - `channel_id` - фильтр по каналу
    - `rule_id` - фильтр по правилу
    - `limit` - лимит результатов (по умолчанию 100)

### Статистика
- `GET /api/stats` - общая статистика системы

### Telegram
- `POST /api/telegram` - отправить сообщение через бота
  - Body: `{ chat_id: string, text: string }`

### Сканирование
- `GET /api/scan` - запуск сканирования каналов (используется cron)

## Переменные окружения

- `TELEGRAM_BOT_SERBIA` - токен Telegram бота для отправки уведомлений
- `TELEGRAM_NOTIFICATION_CHAT_ID` - ID чата/канала для отправки уведомлений о найденных совпадениях (опционально, можно указать в настройках)

## Структура проекта

```
telegram-channel-monitor/
├── pages/
│   ├── _app.js          # Главный компонент приложения
│   ├── index.js         # Главная страница
│   └── api/             # API endpoints
│       ├── channels.js  # Управление каналами
│       ├── rules.js     # Управление правилами
│       ├── matches.js   # Просмотр совпадений
│       ├── stats.js     # Статистика
│       ├── scan.js      # Сканирование (cron)
│       └── telegram.js  # Telegram API
├── src/
│   ├── components/      # React компоненты
│   │   ├── ChannelManager.js
│   │   ├── RuleManager.js
│   │   ├── MatchesList.js
│   │   └── StatsPanel.js
│   └── styles/
│       └── global.css   # Глобальные стили
├── mock-data/           # Mock данные для демо
│   ├── channels.js
│   ├── rules.js
│   └── matches.js
├── package.json
├── vercel.json          # Конфигурация Vercel
├── next.config.js       # Конфигурация Next.js
└── README.md
```

## Типы правил поиска

1. **keywords** - поиск по ключевым словам (через запятую)
   - Пример: `python, telegram, bot`

2. **regex** - регулярные выражения
   - Пример: `\b(?:python|javascript)\b`

3. **semantic** - семантический поиск (в разработке)

## Особенности

- ✅ Инкрементальная загрузка сообщений
- ✅ Автоматическое сканирование каждый час (через Vercel Cron)
- ✅ Фильтрация результатов
- ✅ Интеграция с Telegram ботом
- ✅ Адаптивный дизайн

## Демо версия

В текущей версии используются mock данные. Для полноценной работы необходимо:

1. Настроить Telegram Bot API
2. Реализовать логику сканирования каналов
3. Подключить базу данных для хранения данных

## Лицензия

MIT

